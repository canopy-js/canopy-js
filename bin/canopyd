#!/bin/sh

CANOPY_LOCATION=$(readlink -f $(which canopy) | xargs dirname | xargs dirname)

nodemon --no-stdin --config $CANOPY_LOCATION/nodemon.json --watch $CANOPY_LOCATION $CANOPY_LOCATION/cli/cli.js "$@"
