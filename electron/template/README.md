# Canopy Electron App

This generated app wraps a Canopy static build in Electron.

Useful commands:

```sh
npm run start
npm run package
npm run make
npm run portable
```

The portable script creates a Windows `.exe`, a Linux `.AppImage`, or the normal packaged macOS `.app` for the current platform.
